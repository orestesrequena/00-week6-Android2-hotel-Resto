<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.fiche.RestaurantFicheActivity">

    <ImageView
        android:id="@+id/fiche_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        tools:src="@drawable/photo_restaurant"
        />

    <TextView
        android:id="@+id/fiche_nom_restaurant"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        tools:text="Title Restaurant"
        android:textSize="24sp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/fiche_categorie_restaurant"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="left"
            android:text="Categorie Restaurant"
            android:textSize="16dp"/>

        <Button
            android:layout_marginTop="16dp"
            android:id="@+id/fiche_email"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bouton_email"
            tools:text="Email"
            android:textColor="@android:color/white"
          />
        <Button
            android:layout_marginTop="16dp"
            android:id="@+id/fiche_phone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bouton_phone"
            tools:text="Phone"
            android:textColor="@android:color/white"/>
        <Button
            android:layout_marginTop="16dp"
            android:id="@+id/fiche_url"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bouton_url"
            tools:text="Url"
            android:textColor="@android:color/white"/>


    </LinearLayout>


</LinearLayout>